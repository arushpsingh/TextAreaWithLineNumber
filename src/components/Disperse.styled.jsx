import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const StyledContainer = styled(Box)`
    width: 60vw;
`;

export const StyledLeftText = styled(Box)`
    text-align: left;
    color: grey;
    font-size: 0.8rem;
    font-weight: 500;
`